# Publicação contínua a partir do GitHub ou CLI Netlify.
[build]
  command = "python -c \"import shutil, os; os.makedirs('dist', exist_ok=True); shutil.copy('index.html', 'dist/index.html')\""
  publish = "dist"
